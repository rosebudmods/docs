---
import { CardGrid } from '@astrojs/starlight/components';
import ModCard from './ModCard.astro';
import { mods } from '@modrinth';
---

<CardGrid>
    {Object.values(mods).map((mod) => <ModCard {mod} />)}
</CardGrid>
